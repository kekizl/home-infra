version: '3.8'

services:
  immich-server:
    container_name: immich_server
    image: ghcr.io/immich-app/immich-server:v2.5.6
    volumes:
      - /mnt/Pool/App_Storage/immich/data:/usr/src/app/upload
      - /etc/localtime:/etc/localtime:ro
    env_file:
      - /mnt/Pool/home-infra/home-infra/nas/nas-services/.env
    ports:
      - 2283:2283
    depends_on:
      - redis
      - database
    restart: always

  immich-machine-learning:
    container_name: immich_machine_learning
    image: ghcr.io/immich-app/immich-machine-learning:v2.5.6
    env_file:
      - /mnt/Pool/home-infra/home-infra/nas/nas-services/.env
    volumes:
      - model-cache:/cache
    restart: always

  redis:
    container_name: immich_redis
    image: docker.io/valkey/valkey:9.0.0
    restart: always

  database:
    container_name: immich_postgres
    image: ghcr.io/immich-app/postgres:14-vectorchord0.4.3-pgvectors0.2.0
    env_file:
      - /mnt/Pool/home-infra/home-infra/nas/nas-services/.env
    environment:
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_USER: ${DB_USERNAME}
      POSTGRES_DB: ${DB_DATABASE_NAME}
      POSTGRES_INITDB_ARGS: '--data-checksums'
    volumes:
      - /mnt/Pool/App_Storage/immich/immich_postgres:/var/lib/postgresql/data
    restart: always

  audiobookshelf:
    container_name: audiobookshelf
    image: ghcr.io/advplyr/audiobookshelf:2.32.1
    ports:
      - 30067:80
    volumes:
      - /mnt/Pool/App_Storage/Audiobookshelf/audiobooks:/audiobooks
      - /mnt/Pool/App_Storage/Audiobookshelf/podcasts:/podcasts
      - /mnt/Pool/App_Storage/Audiobookshelf/config:/config
      - /mnt/Pool/App_Storage/Audiobookshelf/metadata:/metadata
    environment:
      - TZ=Europe/Stockholm
    restart: unless-stopped

volumes:
  model-cache:
